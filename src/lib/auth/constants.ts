/** Alinhado à política de senha do projeto Supabase (Auth → Providers). */
export const MIN_PASSWORD_LENGTH = 8

export const WEAK_PASSWORD_MESSAGE = `Senha muito fraca. Use pelo menos ${MIN_PASSWORD_LENGTH} caracteres.`
